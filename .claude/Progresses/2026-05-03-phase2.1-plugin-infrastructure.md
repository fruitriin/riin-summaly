# 進捗表（アーカイブ） — phase2.1 plugin infrastructure

## タスク

phase2.1 — プラグイン基盤の整備（getJson ヘルパ・プラグイン name 定数・BROWSER_UA・KNOWN_SHORT_HOSTS dispatcher）。
phase3.1 / phase3.2 / phase6.1 のプラグイン群が依存する横断的な共通基盤を先に固める。

計画: [docs/plans/phase2.1-plugin-infrastructure.md](../../docs/plans/phase2.1-plugin-infrastructure.md)

## サブタスク（実施結果）

- [x] **Step 1 — `getJson` 追加** ([src/utils/got.ts](../../src/utils/got.ts))
  - `getJson(url, referer?, opts?)` を追加。`getResponse` 経由で SSRF ガードを継承
  - typeFilter で JSON 系 content-type のみ許可、`String(res.body)` で安全に変換
  - User-Agent デフォルト（`DEFAULT_BOT_UA`）+ `opts.userAgent` で上書き可能（コードレビュー指摘対応）
- [x] **Step 2 — プラグイン name 導入**
  - [src/iplugin.ts](../../src/iplugin.ts) に `name?: string` を optional 追加
  - amazon / bluesky / wikipedia / branchio-deeplinks に `export const name`
  - CI テスト: `readdirSync` でファイル名と name の一致を検証
- [x] **Step 3 — UA オーバーライド** ([src/utils/user-agents.ts](../../src/utils/user-agents.ts))
  - `BROWSER_UA = 'Mozilla/5.0 ... Chrome/130 ...'` を export（2026-05-03 更新コメント付き）
- [x] **Step 4 — KNOWN_SHORT_HOSTS** ([src/utils/short-urls.ts](../../src/utils/short-urls.ts))
  - youtu.be / amzn.to / amzn.asia / a.co / w.wiki / spotify.link を Set として export
- [x] **Step 5 — dispatcher 改修** ([src/index.ts](../../src/index.ts))
  - `opts.followRedirects || KNOWN_SHORT_HOSTS.has(initialHost)` で HEAD 解決
  - `maxRedirects: 5` で多段リダイレクトを制限（コードレビュー指摘対応）
- [x] **品質ゲート Stage 1**: pnpm build / eslint / test (82 passed, 1 skipped、既存 lint warning は無関係)、ADDF テスト全 passed
- [x] **品質ゲート Stage 2**: addf-code-review-agent
  - Critical/High なし
  - Warning 級 3 件すべて修正: getJson の UA 欠落・body キャスト・HEAD リダイレクト未制限
  - Info: BROWSER_UA を Chrome 130 に更新、コメント整備
  - addf-contribution-agent はスキップ（変更が `.claude/` `docs/knowhow/ADDF/` `templates/` を含まないバグ修正・基盤整備のため、Feedback.md の判断ガイドに沿う）

## 知見

- **コーディング知見**: [docs/knowhow/plugin-infrastructure-patterns.md](../../docs/knowhow/plugin-infrastructure-patterns.md) — getJson / name / BROWSER_UA / KNOWN_SHORT_HOSTS の設計判断と SSRF 防御パターン
- **タスク総括**: 既存 `getResponse` を再利用するヘルパは SSRF ガードを自動継承できるので新規実装より圧倒的に安全。ただし「経由するから自動的に安全」という思い込みで User-Agent や typeFilter のような明示設定を漏らしやすく、コードレビューが effective だった。次回同種の「コア API ラッパ追加」では、`scpaping` の振る舞いを参照テンプレートにすると漏れが減る

## 完了

完了日: 2026-05-03
コミット: phase2.1 として 1 コミットにまとめる
