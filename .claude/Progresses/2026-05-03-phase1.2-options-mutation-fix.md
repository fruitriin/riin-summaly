# 進捗表（アーカイブ） — phase1.2 options mutation fix

## タスク

phase1.2 — `summaly()` の `Object.assign(summalyDefaultOptions, options)` がモジュール定数を mutate するバグを修正する。phase1.1 のテスト追加中に発見された独立バグ（バグ分離）。

計画: [docs/plans/phase1.2-options-mutation-fix.md](../../docs/plans/phase1.2-options-mutation-fix.md)

## サブタスク（実施結果）

- [x] **Step 1 — src/index.ts の修正**: `Object.assign(summalyDefaultOptions, options)` → `{ ...summalyDefaultOptions, ...options }`（1 行置換）
- [x] **Step 2 — 回帰テスト追加**: 新規 describe `options 不変性` に 2 件追加
  - 連続呼び出しで前回 opts が漏れないこと
  - `summalyDefaultOptions` 自体が呼び出しで mutate されないこと
- [x] **Step 3 — phase1.1 回避策の撤去**: `setupOriginAndProxy` および 500 エラーテストから `contentLengthLimit: 10 * 1024 * 1024` の明示渡しを撤去
- [x] **Step 4 — CHANGELOG 追記**: `(unreleased)` セクションに変更点を追記
- [x] **品質ゲート Stage 1**: pnpm build / eslint / test (73 passed, network test 1 件 flake は無関係)、ADDF テスト全 passed
- [x] **品質ゲート Stage 2**: addf-code-review-agent / addf-contribution-agent
  - Critical/High/Medium 指摘なし
  - Suggestion レベル: `Object.freeze` は計画オープンクエスチョン通りスコープ外、`plugins: []` の配列共有は読み取り専用パスで実害なし
  - Contribution 候補なし（プロジェクト固有のバグ修正）

## 知見

`/addf-knowhow` で記録した内容:

- **コーディング知見**: [docs/knowhow/object-assign-mutable-target.md](../../docs/knowhow/object-assign-mutable-target.md) — `Object.assign(constant, override)` アンチパターンと検出・回帰テスト方法
- **タスク総括**: 1 行修正＋テスト追加で完結する XS タスクは Stage 1 / Stage 2 ゲートも軽量。ただし「バグ分離」由来のタスクは前 phase の回避策撤去が暗黙の Step として必須なので、計画ファイルに明記しておくと取りこぼしが減る（本計画の Step 3 はその好例）

## 完了

完了日: 2026-05-03
コミット: phase1.2 として 1 コミットにまとめる
