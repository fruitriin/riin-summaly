# 進捗表（アーカイブ） — phase3.1 oEmbed 系プラグイン

## タスク

phase3.1 — mei23 fork から oEmbed 直叩きプラグイン (youtube / spotify) を取り込み、Misskey の Note プレビューで頻出する URL のレイテンシを削減する。

計画: [docs/plans/phase3.1-plugin-oembed.md](../../docs/plans/phase3.1-plugin-oembed.md)

## サブタスク（実施結果）

- [x] **Step 3 → Step 1 → Step 2 の順で実装**: 共通定数 `PLAYER_ALLOW_OEMBED` を先に固めると後続プラグインが import するだけで済む
- [x] **Step 3 — `PLAYER_ALLOW_OEMBED`** ([src/utils/player-allow.ts](../../src/utils/player-allow.ts))
  - `readonly string[]` + `Object.freeze()` で mutate 防止（コードレビュー W-3 反映）
- [x] **Step 1 — youtube プラグイン** ([src/plugins/youtube.ts](../../src/plugins/youtube.ts))
  - `(www|m).youtube.com/{watch,v,playlist,shorts}` および `youtu.be` をハンドル
  - `getJson` で oEmbed を 1 リクエスト取得
  - `buildSummaryFromOEmbed(oEmbed: unknown)` をヘルパとして export し、フィクスチャテストを実現（コードレビュー W-2 反映）
  - iframe src は `new URL(src).protocol !== 'https:'` で検証（コードレビュー W-1 反映）
- [x] **Step 2 — spotify プラグイン** ([src/plugins/spotify.ts](../../src/plugins/spotify.ts))
  - `open.spotify.com` をハンドル、同じ `buildSummaryFromOEmbed` パターン
  - branchio-deeplinks の `spotify.link` ルートと衝突しない
- [x] **プラグイン登録** ([src/plugins/index.ts](../../src/plugins/index.ts))
  - 末尾に追加（順序的には branchio-deeplinks 後）
- [x] **テスト追加**: test() URL マッチング 2 件 + `PLAYER_ALLOW_OEMBED` 内容確認 1 件 + `buildSummaryFromOEmbed` フィクスチャテスト 8 件、計 11 件
  - 既存ネットワークテスト「Stage Bye Stage」を新仕様に合わせて簡素化（structure-only assertion で HTML 変化に強く）
- [x] **Step 4 — README / CHANGELOG**
  - README example の出力 JSON を新仕様 (icon=favicon.ico, description=null) に更新
  - 既存挙動から逸脱する旨と回避方法（`allowedPlugins` で youtube/spotify を除外）を README に追記
- [x] **品質ゲート Stage 1**: pnpm build / eslint / test (108 passed, 1 skipped)、ADDF テスト全 passed
- [x] **品質ゲート Stage 2**: addf-code-review-agent
  - Critical/High なし
  - Warning 3 件すべて修正:
    - W-1: `startsWith('https://')` → `new URL(src).protocol !== 'https:'`
    - W-2: フィクスチャテストなし → `buildSummaryFromOEmbed` をヘルパに切り出して 8 件追加
    - W-3: `PLAYER_ALLOW_OEMBED` mutable → `readonly` + `Object.freeze()`、`Summary.player.allow` への代入はスプレッドコピー
  - Suggestion 修正: S-3 (PLAYER_ALLOW_OEMBED テストの中身を実装)
  - addf-contribution-agent はスキップ（Feedback.md ガイド準拠、`.claude/` `docs/knowhow/ADDF/` `templates/` を含まない）

## 知見

- **コーディング知見**: [docs/knowhow/plugin-infrastructure-patterns.md](../../docs/knowhow/plugin-infrastructure-patterns.md) に「oEmbed プラグインのテスト容易化」「iframe URL parse 検証」「PLAYER_ALLOW_OEMBED の readonly 共有」の節を追記
- **タスク総括**:
  - **挙動変更を伴うプラグイン置換** は CHANGELOG / README で明示的に「以前との差分」と「回避策」(allowedPlugins で除外) をセットで提示すると利用者の困惑が減る
  - **3 段ロジック (network → JSON → 組立) のテスト容易化** は組立部分を pure function に切り出すパターンが効く。`vi.mock` よりずっと書きやすく、フィクスチャベースの異常系網羅もできる
  - **コードレビューの W-2「フィクスチャテスト欠如」を catch できた**のは、計画書の Definition of Done で明示的に「フィクスチャベースのテスト」を要求していたから。Plan 段階での DoD 明文化が効いた

## 完了

完了日: 2026-05-03
コミット: phase3.1 として 1 コミットにまとめる
