# 進捗表（アーカイブ） — phase2.2 mei23 非プラグイン取り込み

## タスク

phase2.2 — mei23 fork から非プラグイン機能（`useRange` / keep-alive agent / `allowedPlugins` / `sanitize-url` / `medias[]` / 文字コード判定強化 / deploy 例）を取捨選択して取り込む。

計画: [docs/plans/phase2.2-mei23-non-plugin.md](../../docs/plans/phase2.2-mei23-non-plugin.md)

## サブタスク（実施結果）

- [x] **Step 1 — `Summary.medias?: string[]` 型追加** ([src/summary.ts](../../src/summary.ts))
- [x] **Step 2 — keep-alive agent 既定化** ([src/utils/agent.ts](../../src/utils/agent.ts))
  - `defaultHttpAgent` / `defaultHttpsAgent` を `keepAlive: true` で生成
  - `SUMMALY_FAMILY=4`/`=6` で IP family 強制
  - `destroyDefaultAgents()` cleanup を `afterAll` で呼ぶことでテストプロセスのハング防止
- [x] **Step 3 — `useRange` オプション** ([src/utils/got.ts](../../src/utils/got.ts), [src/general.ts](../../src/general.ts), [src/index.ts](../../src/index.ts))
  - `Range: bytes=0-N-1` ヘッダ送信。サーバ未対応なら 200 OK でフルボディに自然フォールバック
  - **実装中の発見**: `summaly` → `general` → `scpaping` への opts 透過チェーンで漏れがあり、テストが失敗。general.ts 側にも `useRange: opts?.useRange` を追加して修正
- [x] **Step 4 — `allowedPlugins` オプション** ([src/index.ts](../../src/index.ts))
  - `undefined` = 全有効、`string[]` = オプトイン許可リスト、`[]` = 組み込み全 disable
  - 外部 `opts.plugins` はカスタム性尊重でフィルタしない
- [x] **Step 5 — `sanitize-url` 結果フィルタ** ([src/utils/sanitize-url.ts](../../src/utils/sanitize-url.ts))
  - `https:` / `http:` / `data:`<10KB のみ通過。`javascript:` 等は null
  - `data:` URI のサイズ判定はバイト長 (`Buffer.byteLength`) で実施（コードレビュー指摘）
  - `summaly()` 出口で集約適用。`player.url` が null になったら `player` 全体をリセット（コードレビュー指摘）
- [x] **Step 6 — 文字コード判定強化** ([src/utils/encoding.ts](../../src/utils/encoding.ts))
  - `chardet` → `jschardet` (confidence 0.99) + `<meta charset>` フォールバック + `utf-8` 最終フォールバック
  - ISO-2022-JP は `encoding-japanese` 経由（[issue #39](https://github.com/misskey-dev/summaly/issues/39) 修正）
  - 依存変更: `chardet` 削除、`jschardet` `encoding-japanese` 追加
- [x] **Step 7 — deploy 補助ファイル** ([docs/deploy-examples/](../../docs/deploy-examples/))
  - nginx reverse proxy / systemd unit / 設定 JSON サンプル + README
- [x] **Step 8 — README / CHANGELOG 更新** ([README.md](../../README.md), [CHANGELOG.md](../../CHANGELOG.md))
  - 新オプション (`useRange` / `allowedPlugins` / `medias`) の表追記
  - keep-alive デフォルト agent / SUMMALY_FAMILY の説明追加
  - "Production deployment" 節を新設
- [x] **Step 9 — テスト追加** ([test/index.test.ts](../../test/index.test.ts))
  - 新規 describe `phase2.2 mei23 取り込み`: allowedPlugins (4) / useRange (2) / sanitize-url (5) / encoding (3) / medias (1) — 計 15 件
  - `afterAll` で `destroyDefaultAgents()` を呼ぶ
- [x] **品質ゲート Stage 1**: pnpm build / eslint / test (97 passed, 1 skipped、既存 lint warning 解消)、ADDF テスト全 passed
- [x] **品質ゲート Stage 2**: addf-code-review-agent
  - Critical/High なし
  - Warning 2 件すべて修正:
    - W-1: `player.url` が null になっても allow が残る → player 全体リセット
    - W-2: SSRF ガードと agent fallback ロジック重複 → `isExternalAgentSet()` ヘルパに集約
  - Suggestion 修正: S-1 (バイト長判定)、S-3 (コメント追加)
  - addf-contribution-agent はスキップ（Feedback.md ガイド準拠、`.claude/` `docs/knowhow/ADDF/` `templates/` を含まない）

## 知見

- **コーディング知見**: [docs/knowhow/sanitize-and-agent-patterns.md](../../docs/knowhow/sanitize-and-agent-patterns.md) — sanitize / keep-alive agent / useRange / allowedPlugins の設計判断
- **タスク総括**: M サイズの 8 ステップ Plan を 1 サイクルで完遂可能。鍵は (1) Step 単位を 1 ショットで終わるサイズに細分化されている計画書、(2) 依存追加は早めに済ます (pnpm install を Step 6 冒頭で実行)、(3) 透過チェーン（opts → general → scpaping）の漏れはテストで catch する仕組みを当初から組み込む。コードレビューで Player リセットや SSRF 判定の集約など「単発の見落とし」が複数 catch されたため、レビュー gate の費用対効果が大きい

## 完了

完了日: 2026-05-03
コミット: phase2.2 として 1 コミットにまとめる
